# Правила для ИИ-программистов (AI_RULES.md)

## 🚫 Анти-монолит правила

### Максимальные размеры файлов
| Тип файла | Макс. строк | Макс. размер |
|-----------|-------------|--------------|
| Компонент (.tsx) | 200 | 10KB |
| Утилита/хук (.ts) | 150 | 8KB |
| Данные (.ts/.json) | 500 | 30KB |
| Страница (page.tsx) | 150 | 10KB |

### Правила декомпозиции

1. **Компоненты**:
   - Один компонент = одна ответственность
   - Если JSX > 100 строк — выноси части в отдельные компоненты
   - UI-элементы (кнопки, карточки) → `components/ui/`
   - Бизнес-компоненты → `components/features/`

2. **Данные**:
   - ❌ ЗАПРЕЩЕНО хранить большие массивы данных в `.ts` файлах
   - ✅ Используй JSON файлы и динамический импорт
   - ✅ Для > 50 записей — используй API или базу данных
   - Пример: `cars_imported_db.ts` (99KB) → разбить на JSON по брендам

3. **Стили**:
   - Не дублируй Tailwind классы — выноси в `@apply` или компоненты
   - Длинные строки классов → переменные или объекты

4. **Типы**:
   - Общие типы → `types/`
   - Локальные типы → в том же файле, но наверху

### Структура директорий

```
components/
├── ui/           # Переиспользуемые UI (Button, Modal, Card)
├── features/     # Бизнес-компоненты (CarCatalog, Calculator)
├── layout/       # Header, Footer, Navbar
data/
├── brands/       # Данные по брендам (JSON)
├── static/       # Статические данные
lib/
├── hooks/        # Кастомные хуки
├── utils/        # Утилиты и хелперы
├── api/          # API клиенты
```

### Чек-лист перед коммитом

- [ ] Нет файлов > 200 строк без явной причины
- [ ] Нет дублирования кода
- [ ] Компоненты можно понять за 30 секунд
- [ ] Нет закомментированного кода
- [ ] Нет временных файлов (temp_*.tsx)

### Рефакторинг существующих монолитов

**Текущие проблемы:**
1. `data/cars_imported_db.ts` — **4485 строк, 99KB** ⚠️
   - Действие: Конвертировать в JSON, разбить по брендам
   - Приоритет: Высокий

## 🚀 Скорость разработки

### Быстрый онбординг
- Каждый файл должен быть понятен за < 1 минуту
- Используй JSDoc комментарии для сложной логики
- README в каждой директории с > 5 файлами

### Быстрый деплой
- Используй `.dockerignore` (уже добавлен)
- Не коммить `node_modules`, `.next`, логи
- Версии синхронизировать в `package.json` и `site.json`
